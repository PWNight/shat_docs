---
title: API v1 vs v2
description: Историческое сравнение legacy API v1 и API v2 (продукт v1.X).
update_date: 18-05-2026
---

> **Актуальное API** — **[API v3](/wiki/api-reference)** (продукт v2.X). Сравнение v2 → v3: [API v2 vs v3](/wiki/api-v2-v3).  
> Здесь описан переход **v1 → v2**, актуальный для эпохи продукта **v1.X**.

## Главные отличия

### 1) Единая инфраструктура ответов в v2

В `v2` введён набор утилит:

- `successResponse(...)`, `errorResponse(...)`;
- `badRequest(...)`, `notFound(...)`, `unauthorized(...)`, `serverError(...)`;
- `handleApiError(...)`.

Это даёт предсказуемые ответы и единый формат ошибок во всех роутингах.

В `v1` обработка ошибок реализована локально в каждом route, из-за чего структура ответа может немного отличаться между эндпоинтами.

### 1.1) Отдельная ветка ошибок доступности БД (v2)

В `v2` ошибки недоступности SQLite детектируются централизованно и возвращаются как:

- HTTP `503`
- `code: "DB_OFFLINE"`
- `message: "Нет подключения к базе данных"`

Это позволяет UI и интеграциям **отличать “сервис временно недоступен”** от обычной ошибки запроса и показывать корректный fallback.

### 2) Безопасный парсинг JSON и валидация

В `v2` добавлены:

- `safeParseJson(request)` - защита от невалидного JSON;
- `validateData(schema, data)` - унифицированная валидация через Zod.

В `v1` JSON и валидация обычно обрабатываются напрямую в каждом обработчике.

### 3) Авторизация: cookie + Bearer в v2

`v2` поддерживает два источника авторизации:

- сессия из cookie;
- `Authorization: Bearer <token>`.

Это реализовано через `extractSession(...)` и особенно полезно для внешних клиентов (боты, сервисы, интеграционные скрипты).

В `v1` маршруты ориентированы в основном на cookie-сессию.

### 4) Изменения в auth/register

В `v2` регистрация работает как **заявка на доступ**:

- создается пользователь со статусом `pending`
- вход в аккаунт невозможен до подтверждения администратором
- админ подтверждает/отклоняет заявку через `PATCH /api/admin/registrations/{id}` с body `status`

В `v1` поведение регистрации и структура ответов отличались между endpoints (и не было единого admin-flow подтверждения заявок).

---

## Что изменилось после v2

Поколение **API v3** (продукт v2.X) наследует v2, но ужесточает доступ к группам, меняет тексты auth и добавляет `/health`. Подробно: [API v2 vs v3](/wiki/api-v2-v3).
